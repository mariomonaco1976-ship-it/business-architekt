---
// Final Design: Elegant / Premium Concept
---

<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Business Architekt – Human-System-Fit</title>
<link rel="icon" type="image/svg+xml" href="/ba_logo_baum.svg?v=2">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<style is:global>
  /* ============================================================
     RESET & BASE
  ============================================================ */
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --primary:       #100746;
    --primary-dark:  #0a0430;
    --gold:          #97832C;
    --signal:        #FF5757;
    --bg:            #F7F6F2;
    --text:          #1A1A1A;
    --divider:       #E6E4DC;
    --card-bg:       #ffffff;
    --card-alt:      #2A265C;
    --card-alt-text: #F7F6F2;
    --font-head:     'Crimson Pro', Georgia, serif;
    --font-body:     'Source Sans 3', 'IBM Plex Sans', system-ui, sans-serif;
    --max-w:         1200px;
    --gap:           clamp(1rem, 2.5vw, 1.75rem);
    --section-gap:   clamp(4rem, 8vw, 7rem);
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: var(--font-body);
    font-size: clamp(16px, 1.1vw + 12px, 18px);
    line-height: 1.65;
    color: var(--text);
    background-color: var(--bg);
    background-image:
      linear-gradient(rgba(16,7,70,0.045) 1px, transparent 1px),
      linear-gradient(90deg, rgba(16,7,70,0.045) 1px, transparent 1px);
    background-size: 28px 28px;
    min-height: 100vh;
  }

  /* ============================================================
     TYPOGRAPHY
  ============================================================ */
  h1 {
    font-family: var(--font-head);
    font-size: clamp(38px, 5vw, 56px);
    font-weight: 700;
    line-height: 1.12;
    letter-spacing: -0.02em;
  h2 {
    font-family: var(--font-head);
    font-size: clamp(26px, 3vw, 36px);
    font-weight: 600;
    line-height: 1.2;
    color: var(--primary);
    letter-spacing: -0.01em;
  }
  h3 {
    font-family: var(--font-head);
    font-size: clamp(18px, 1.8vw, 24px);
    font-weight: 600;
    line-height: 1.3;
    color: var(--primary);
  }
  p { max-width: 68ch; }
  small, .meta {
    font-size: clamp(13px, 0.9vw + 10px, 15px);
    color: #6B6860;
    line-height: 1.5;
  }
  a { color: var(--gold); text-decoration: underline; text-underline-offset: 3px; }
  a:hover { color: var(--primary); }

  /* ============================================================
     LAYOUT HELPERS
  ============================================================ */
  .container {
    width: 100%;
    max-width: var(--max-w);
    margin-inline: auto;
    padding-inline: clamp(1.25rem, 5vw, 3rem);
  }

  .section { padding-block: var(--section-gap); }

  .divider-line {
    border: none;
    border-top: 1px solid var(--divider);
    margin-block: 0;
  }

  /* ============================================================
     HEADER / NAV
  ============================================================ */
  .site-header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(247,246,242,0.92);
    backdrop-filter: blur(8px);
    border-bottom: 1px solid var(--divider);
  }
  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding-block: 1.1rem;
  }
  .logo {
    font-family: var(--font-head);
    font-size: 1.05rem;
    font-weight: 700;
    color: var(--primary);
    text-decoration: none;
    letter-spacing: 0.02em;
    display: flex;
    flex-direction: column;
    gap: 0;
  }
  .logo span {
    font-size: 0.72rem;
    font-weight: 400;
    color: var(--gold);
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }
  .header-nav {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.35rem 1rem;
    flex-wrap: wrap;
  }
  .header-nav a {
    font-size: 0.8rem;
    text-decoration: none;
    color: var(--primary);
    opacity: 0.75;
    white-space: nowrap;
  }
  .header-nav a:hover { opacity: 1; text-decoration: underline; text-underline-offset: 4px; }
  .nav-cta {
    display: inline-block;
    background: #fff;
    color: var(--gold);
    font-family: var(--font-body);
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    padding: 0.55rem 1.25rem;
    border: 1px solid var(--gold);
    border-radius: 8px;
    transition: background 0.2s;
  }
  .nav-cta:hover { background: #f7f3e6; color: var(--gold); }

  /* ============================================================
              <strong>Für Unternehmer, die schnell entscheiden müssen — und kein Lehrgeld bezahlen wollen.</strong>
            </p>
          </div>

          <div class="hero-actions">
            <a href="#start" class="btn-primary">Human-System-Fit starten</a>
    font-size: clamp(16px, 1.4vw, 19px);
    line-height: 1.55;
    color: #3a3830;
    max-width: 72ch;
    text-wrap: pretty;
  }
  .hero-sub-line,
  .hero-sub-end {
    display: block;
  }
  .hero-sub-end {
    color: var(--primary);
    font-weight: 600;
  }
  .hero-sub strong { color: var(--primary); font-weight: 600; }
  .hero-levers {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--gap);
    margin-top: 0.5rem;
  }
  .hero-lever {
    border-left: 2px solid var(--gold);
    padding-left: 1rem;
  }
  .hero-lever strong {
    display: block;
    font-size: 0.85rem;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--primary);
    margin-bottom: 0.25rem;
  }
  .hero-lever p {
    font-size: 0.88rem;
    color: #5a5750;
    line-height: 1.5;
    max-width: none;
  }
  .hero-thesis {
    background: var(--primary);
    color: #d4c86a;
    padding: 1.5rem 1.75rem;
    max-width: 640px;
  }
  .hero-thesis p {
    font-size: clamp(15px, 1.2vw, 17px);
    line-height: 1.65;
    max-width: none;
  }
  .hero-thesis p strong { color: #d4c86a; }
  .hero-actions { display: flex; align-items: center; gap: 1.5rem; flex-wrap: wrap; }
  .btn-primary {
    display: inline-block;
    background: var(--primary);
    color: #d4c86a;
    font-family: var(--font-body);
    font-size: 0.95rem;
    font-weight: 700;
    text-decoration: none;
    padding: 0.85rem 2rem;
    border: 1px solid var(--primary);
    transition: background 0.2s, color 0.2s, border-color 0.2s;
  }
  .btn-primary:hover { background: var(--primary-dark); color: #d4c86a; border-color: var(--primary-dark); }
  .hero-note { font-size: 0.83rem; color: #7a7870; }

  /* ============================================================
     SYMPTOM SECTION
  ============================================================ */
  .symptom-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--gap);
    margin-top: 2.5rem;
  }
  .symptom-card {
    background: var(--card-bg);
    border: 1px solid var(--divider);
    padding: 2rem 1.75rem;
  }
  .symptom-card h3 { margin-bottom: 0.75rem; }
  .symptom-card .desc { font-size: 0.93rem; color: #4a4840; margin-bottom: 1.25rem; max-width: none; }
  .symptom-card ul { list-style: none; padding: 0; }
  .symptom-card ul li {
    font-size: 0.9rem;
    padding: 0.5rem 0;
    border-top: 1px solid var(--divider);
    color: #3a3830;
    padding-left: 1rem;
    position: relative;
  }
  .symptom-card ul li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: var(--signal);
    font-size: 0.75rem;
  }

  /* ============================================================
     ZAHLENBLOCK
  ============================================================ */
  .numbers-grid {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: var(--gap);
    margin-top: 2.5rem;
  }
  .number-card {
    background: var(--card-bg);
    border: 1px solid var(--divider);
    padding: 2rem 1.75rem;
  }
  .number-card:nth-child(1) { grid-column: span 5; }
  .number-card:nth-child(2) { grid-column: span 7; }
  .number-card:nth-child(3) { grid-column: span 4; }
  .number-card:nth-child(4) { grid-column: span 8; }
  .number-big {
    font-family: var(--font-head);
    font-size: clamp(42px, 5vw, 64px);
    font-weight: 700;
    color: var(--primary);
    line-height: 1;
    margin-bottom: 0.75rem;
  }
  .number-card p {
    font-size: 0.95rem;
    color: #3a3830;
    line-height: 1.55;
    max-width: 90%;
    margin-bottom: 1rem;
  }
  .number-source {
    font-size: 0.78rem;
    color: #8a8880;
    border-top: 1px solid var(--divider);
    padding-top: 0.75rem;
  }
  .number-source a { color: #8a8880; font-size: inherit; }
  .number-card.card-dark {
    background: var(--card-alt);
    border-color: var(--card-alt);
    color: var(--card-alt-text);
  }
  .number-card.card-dark .number-big { color: #d4c86a; }
  .number-card.card-dark p { color: #ccc9e0; }
  .number-card.card-dark .number-source { color: #9995b0; border-color: rgba(255,255,255,0.1); }
  .number-card.card-dark .number-source a { color: #9995b0; }

  /* ============================================================
     SYSTEM VISUALISIERUNG
  ============================================================ */
  .system-section { background: var(--primary); color: #F7F6F2; }
  .system-section h2 { color: #F7F6F2; }
  .system-section .section-intro { color: #b8b5d0; max-width: 58ch; margin-top: 0.75rem; }

  .orbit-wrapper {
    position: relative;
    width: min(560px, 90vw);
    height: min(560px, 90vw);
    margin: 3.5rem auto 0;
  }
  .orbit-center {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    width: 120px; height: 120px;
    border-radius: 50%;
    background: rgba(212,200,106,0.12);
    border: 2px solid var(--gold);
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    font-family: var(--font-head);
    font-size: 1.05rem;
    font-weight: 700;
    color: #d4c86a;
    line-height: 1.3;
    z-index: 2;
    cursor: pointer;
    transition: all 0.35s ease;
  }
  .orbit-center:hover {
    width: 180px;
    height: 180px;
    background: rgba(16,7,70,0.95);
    border-color: #d4c86a;
    font-size: 0.82rem;
    line-height: 1.45;
  }
  .orbit-center .center-main { transition: opacity 0.2s; }
  .orbit-center:hover .center-main { opacity: 0; }
  .orbit-center .center-hover {
    position: absolute;
    opacity: 0;
    padding: 1.2rem;
    transition: opacity 0.2s 0.15s;
    font-weight: 400;
  }
  .orbit-center:hover .center-hover { opacity: 1; }
  .orbit-ring {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    width: 430px; height: 430px;
    max-width: 78vw; max-height: 78vw;
    border-radius: 50%;
    border: 1px solid rgba(212,200,106,0.2);
  }

  /* Six orbit cards positioned at clock positions */
  .orbit-card {
    position: absolute;
    width: 130px;
    background: rgba(42,38,92,0.9);
    border: 1px solid rgba(212,200,106,0.25);
    padding: 0.85rem 0.9rem;
    font-size: 0.8rem;
    line-height: 1.45;
    transition: all 0.28s ease;
    cursor: default;
    overflow: hidden;
    z-index: 3;
  }
  .orbit-card strong {
    display: block;
    font-size: 0.78rem;
    font-weight: 700;
    color: #d4c86a;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    margin-bottom: 0.3rem;
  }
  .orbit-card .card-desc {
    display: none;
    font-size: 0.75rem;
    color: #9995b0;
    margin-top: 0.4rem;
    line-height: 1.5;
  }
  .orbit-card:hover {
    width: 155px;
    background: rgba(16,7,70,0.98);
    border-color: var(--gold);
    z-index: 10;
  }
  .orbit-card:hover .card-desc { display: block; }

  /* Positions: top, top-right, bottom-right, bottom, bottom-left, top-left */
  /* Using percentage positioning relative to orbit-wrapper */
  .orbit-card:nth-child(3) { /* top 12 o'clock */
    top: 2%; left: 50%; transform: translateX(-50%);
  }
  .orbit-card:nth-child(4) { /* 2 o'clock */
    top: 18%; right: 0%; transform: translateX(20%);
  }
  .orbit-card:nth-child(5) { /* 4 o'clock */
    bottom: 18%; right: 0%; transform: translateX(20%);
  }
  .orbit-card:nth-child(6) { /* 6 o'clock */
    bottom: 2%; left: 50%; transform: translateX(-50%);
  }
  .orbit-card:nth-child(7) { /* 8 o'clock */
    bottom: 18%; left: 0%; transform: translateX(-20%);
  }
  .orbit-card:nth-child(8) { /* 10 o'clock */
    top: 18%; left: 0%; transform: translateX(-20%);
  }

  .system-note {
    text-align: center;
    margin-top: 2.5rem;
    font-size: 0.85rem;
    color: rgba(184,181,208,0.7);
    font-style: italic;
  }

  /* ============================================================
     ERGEBNISBEREICH
  ============================================================ */
  .result-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: var(--gap);
    margin-top: 2.5rem;
  }
  .result-card {
    background: var(--card-bg);
    border: 1px solid var(--divider);
    padding: 2rem 1.75rem;
    border-top: 3px solid var(--primary);
  }
  .result-card h3 { margin-bottom: 0.75rem; }
  .result-card p { font-size: 0.93rem; color: #4a4840; line-height: 1.6; max-width: none; }
  .result-thesis {
    margin: var(--gap) auto 0;
    max-width: 860px;
    text-align: center;
  }
  .result-thesis p {
    font-size: clamp(15px, 1.2vw, 17px);
    line-height: 1.65;
    max-width: none;
    margin-top: 0;
  }
  .result-thesis p strong { color: #d4c86a; }
  .result-thesis-label {
    display: block;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #d4c86a;
    margin-bottom: 0.4rem;
  }
  .result-thesis strong {
    display: inline-block;
    font-size: clamp(22px, 2.2vw, 30px);
    margin-bottom: 0.35rem;
  }

  /* KONSEQUENZ */
  .consequence-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--gap);
    margin-top: 2.5rem;
  }
  .consequence-col {
    background: var(--card-bg);
    border: 1px solid var(--divider);
    padding: 2rem 1.75rem;
  }
  .consequence-col.col-before { border-top: 3px solid var(--signal); }
  .consequence-col.col-after  { border-top: 3px solid var(--gold); }
  .consequence-col h3 { margin-bottom: 1.25rem; }
  .consequence-col ul { list-style: none; padding: 0; }
  .consequence-col ul li {
    font-size: 0.93rem;
    padding: 0.5rem 0;
    border-bottom: 1px solid var(--divider);
    color: #3a3830;
    padding-left: 1.25rem;
    position: relative;
  }
  .col-before ul li::before { content: '×'; position: absolute; left: 0; color: var(--signal); font-weight: 700; }
  .col-after  ul li::before { content: '→'; position: absolute; left: 0; color: var(--gold); font-weight: 700; }

  /* ============================================================
     PRICING / CTA
  ============================================================ */
  .pricing-block {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--gap);
    margin-top: 2.5rem;
  }
  .price-card {
    background: var(--card-bg);
    border: 1px solid var(--divider);
    padding: 2rem 1.75rem;
  }
  .price-card.price-active {
    background: var(--primary);
    border-color: var(--primary);
    color: #F7F6F2;
  }
  .price-card.price-active h3 { color: #F7F6F2; }
  .price-tag {
    font-family: var(--font-head);
    font-size: clamp(36px, 4vw, 52px);
    font-weight: 700;
    line-height: 1;
    margin-block: 0.75rem;
  }
  .price-card.price-active .price-tag { color: #d4c86a; }
  .price-label {
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--gold);
    margin-bottom: 0.25rem;
  }
  .price-card.price-active .price-label { color: #d4c86a; }
  .price-card p { font-size: 0.9rem; line-height: 1.6; max-width: none; }
  .price-card.price-active p { color: #b8b5d0; }
  .checkmarks {
    margin-top: 1.25rem;
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }
  .checkmark-item {
    font-size: 0.83rem;
    color: #6a6860;
  }
  .price-card.price-active .checkmark-item { color: #9995b0; }
  .checkmark-item::before { content: '✓  '; color: var(--gold); font-weight: 700; }
  .price-card.price-active .checkmark-item::before { color: #d4c86a; }

  /* ============================================================
     ABSCHLUSS / BOT
  ============================================================ */
  .closing-section {
    border-top: 1px solid var(--divider);
  }
  .closing-inner {
    max-width: 700px;
  }
  .closing-inner h2 { margin-bottom: 1rem; }
  .closing-inner p { color: #4a4840; margin-bottom: 2rem; }
  .bot-placeholder {
    background: var(--card-bg);
    border: 1px solid var(--divider);
    min-height: 320px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #aaa9a0;
    font-size: 0.85rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    margin-top: 2.5rem;
  }

  /* ============================================================
     FOOTER
  ============================================================ */
  .site-footer {
    background: var(--primary);
    color: rgba(247,246,242,0.5);
    padding-block: 2.5rem;
    font-size: 0.83rem;
  }
  .footer-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 1rem;
  }
  .footer-logo {
    font-family: var(--font-head);
    font-size: 1rem;
    font-weight: 700;
    color: rgba(247,246,242,0.8);
  }
  .footer-links { display: flex; gap: 1.5rem; }
  .footer-links a { color: rgba(247,246,242,0.45); text-decoration: none; font-size: 0.8rem; }
  .footer-links a:hover { color: rgba(247,246,242,0.8); }

  /* ============================================================
            <h3>Wirtschaftlicher Druck</h3>
            <p class="desc">Kosten steigen nicht nacheinander, sondern gleichzeitig. Energie, Material, Lohn. Wenn Deine Entscheidung unscharf ist, frisst sie Marge.</p>
            <ul>
              <li>Fehler werden schneller teuer</li>
              <li>Liquidität braucht klarere Prioritäten</li>
              <li>Reibung frisst Marge im Alltag</li>
            </ul>
          </div>
          <div class="symptom-card">
            <h3>Deine Stärke</h3>
            <p class="desc">Konzerne brauchen Freigaben. Du brauchst Klarheit. Genau das ist Dein Vorteil: Du kannst in Tagen entscheiden, wofür andere Monate brauchen.</p>
            <ul>
              <li>Geschwindigkeit statt Abstimmungsschleifen</li>
              <li>Anpassung vor statt nach dem Schaden</li>
              <li>Klare Entscheidungen unter Unsicherheit</li>
            </ul>
          </div>
          <div class="symptom-card">
            <h3>Der eigentliche Hebel</h3>
            <p class="desc">Tempo allein reicht nicht. Vorsicht allein auch nicht. Du gewinnst, wenn Du schnell entscheidest und Risiken vorher sichtbar machst.</p>
            <ul>
              <li>Weniger Aktivismus, mehr Reihenfolge</li>
              <li>Weniger Lehrgeld, mehr Wirkung</li>
              <li>Weniger Stress, mehr Führung</li>
            </ul>
          </div>
    .orbit-card { width: 110px; font-size: 0.75rem; padding: 0.7rem 0.75rem; }
    .orbit-card:hover { width: 130px; }
    .orbit-center { width: 96px; height: 96px; font-size: 0.88rem; }
    .orbit-center:hover { width: 145px; height: 145px; font-size: 0.72rem; }
  }

  @media (max-width: 640px) {
    .header-inner { flex-direction: column; align-items: flex-start; }
    .header-nav { justify-content: flex-start; }
    .hero-line { white-space: normal; }
    .hero-levers { grid-template-columns: 1fr; }
    .numbers-grid { grid-template-columns: 1fr; }
    .number-card:nth-child(n) { grid-column: span 1; }
    .orbit-wrapper { width: 92vw; height: 92vw; }
    .orbit-ring { width: 70vw; height: 70vw; }
    .orbit-card { width: 90px; font-size: 0.68rem; padding: 0.6rem; }
    .orbit-card:hover { width: 108px; }
    .orbit-center { width: 76px; height: 76px; font-size: 0.78rem; }
    .orbit-center:hover { width: 120px; height: 120px; font-size: 0.62rem; }
    .orbit-center .center-hover { padding: 0.8rem; }
    .footer-inner { flex-direction: column; }
  }
</style>
</head>
<body>

<!-- ============================================================
              <strong>Für Unternehmer, die schnell entscheiden müssen — und kein Lehrgeld bezahlen wollen.</strong>
            </p>
          </div>

          <div class="hero-actions">
            <a href="#start" class="btn-primary">Human-System-Fit starten</a>
            <p class="hero-note"><span style="color:#2563eb;">✔</span> klare Einordnung · <span style="color:#2563eb;">✔</span> kein Abo · <span style="color:#2563eb;">✔</span> keine Folgekosten</p>
          </div>

      <p class="hero-sub">
        Ich helfe Unternehmern, Entscheidungslogik, Geschäftsarchitektur und KI-Einsatz so auszurichten, dass weniger Reibung entsteht und mehr tragfähige Wirkung.
      </p>

      <div class="hero-thesis">
        <p>
<<<<<<< HEAD
          <strong>Der Human-System-Fit verbindet beides: Schnell entscheiden. Sauber absichern.</strong>
        </p>
      </div>

      <div class="hero-levers">
        <div class="hero-lever">
          <strong>Umsatz</strong>
          <p>Positionierung, Vertrieb, Marktmechanik</p>
        </div>
        <div class="hero-lever">
          <strong>Kosten</strong>
          <p>Struktur, Prozesse, Komplexität</p>
        </div>
        <div class="hero-lever">
          <strong>Entscheidungen</strong>
          <p>Denklogik, Priorisierung, Führung</p>
        </div>
      </div>

      <div class="hero-actions">
        <a href="#start" class="btn-primary">Human-System-Fit starten</a>
        <p class="hero-note">15–20 Minuten · kein Abo · keine Folgekosten</p>
=======
          <strong>Für Unternehmer, die schnell entscheiden müssen — und kein Lehrgeld bezahlen wollen.</strong>
        </p>
      </div>

      <div class="hero-actions">
        <a href="#start" class="btn-primary">Human-System-Fit starten</a>
        <p class="hero-note"><span style="color:#2563eb;">✔</span> klare Einordnung · <span style="color:#2563eb;">✔</span> kein Abo · <span style="color:#2563eb;">✔</span> keine Folgekosten</p>
>>>>>>> feature/website-update-2026-04-20
      </div>
    </div>
  </div>
</section>

<hr class="divider-line">

<!-- ============================================================
     SYMPTOM BEREICH
============================================================ -->
<section class="section">
  <div class="container">
    <p class="section-label">Warum jetzt</p>
    <h2>Der Markt verzeiht keine langen Umwege mehr</h2>
    <p class="section-intro">Jede falsche Entscheidung kostet dreifach: Geld, Zeit und Energie.</p>

    <div class="symptom-grid">
      <div class="symptom-card">
<<<<<<< HEAD
        <h3>Wirtschaftlicher Druck</h3>
        <p class="desc">Kosten steigen nicht nacheinander, sondern gleichzeitig. Energie, Material, Lohn. Wenn Deine Entscheidung unscharf ist, frisst sie Marge.</p>
        <ul>
          <li>Fehler werden schneller teuer</li>
          <li>Liquidität braucht klarere Prioritäten</li>
          <li>Reibung frisst Marge im Alltag</li>
        </ul>
      </div>
      <div class="symptom-card">
        <h3>Deine Stärke</h3>
        <p class="desc">Konzerne brauchen Freigaben. Du brauchst Klarheit. Genau das ist Dein Vorteil: Du kannst in Tagen entscheiden, wofür andere Monate brauchen.</p>
        <ul>
          <li>Geschwindigkeit statt Abstimmungsschleifen</li>
          <li>Anpassung vor statt nach dem Schaden</li>
          <li>Klare Entscheidungen unter Unsicherheit</li>
        </ul>
      </div>
      <div class="symptom-card">
        <h3>Der eigentliche Hebel</h3>
        <p class="desc">Tempo allein reicht nicht. Vorsicht allein auch nicht. Du gewinnst, wenn Du schnell entscheidest und Risiken vorher sichtbar machst.</p>
        <ul>
          <li>Weniger Aktivismus, mehr Reihenfolge</li>
          <li>Weniger Lehrgeld, mehr Wirkung</li>
          <li>Weniger Stress, mehr Führung</li>
        </ul>
=======
        <h3>Zu viele Tools, zu wenig Ordnung</h3>
        <p class="desc">KI beschleunigt Prozesse. Aber ohne klare Entscheidungslogik beschleunigst du nur Chaos. Und zahlst doppelt: erst für das Tool, dann für die Folgeschäden.</p>
      </div>
      <div class="symptom-card">
        <h3>Zu viel Aktivität, zu wenig Richtung</h3>
        <p class="desc">Nicht jede Möglichkeit ist ein nächster Schritt. Tragfähigkeit entsteht durch Reihenfolge — nicht durch mehr Einsatz. Wer alles gleichzeitig bewegt, kommt nirgendwo schneller an.</p>
      </div>
      <div class="symptom-card">
        <h3>Business passt nicht zur Person, die es führt</h3>
        <p class="desc">Wenn Rolle, Energie, Markt und Struktur nicht zusammenpassen, wird Wachstum teuer. Nicht weil du zu wenig tust — sondern weil du das Falsche tust, mit dem Falschen, zur falschen Zeit.</p>
>>>>>>> feature/website-update-2026-04-20
      </div>
    </div>
  </div>
</section>

<hr class="divider-line">

<!-- ============================================================
     ZAHLENBLOCK
============================================================ -->
<section class="section">
  <div class="container">
<<<<<<< HEAD
    <p class="section-label">Die drei Hebel</p>
    <h2>Ein Unternehmen hat nur drei Stellschrauben</h2>
    <p class="section-intro">Umsatz. Kosten. Entscheidungen. Alles andere ist Wirkung dieser drei Hebel.</p>

    <div class="numbers-grid">
      <div class="number-card">
        <div class="number-big">Umsatz</div>
        <p>Positionierung, Vertrieb, Marktmechanik.</p>
        <p class="number-source">Was sorgt aktuell für tragfähigen Zufluss?</p>
      </div>
      <div class="number-card card-dark">
        <div class="number-big">Entscheidungen</div>
        <p>Reihenfolge, Klarheit, Führung.</p>
        <p class="number-source">Triffst Du Entscheidungen mit tragfähiger Grundlage?</p>
      </div>
      <div class="number-card card-dark">
        <div class="number-big">Kosten</div>
        <p>Struktur, Prozesse, Komplexität.</p>
        <p class="number-source">Wo wird Leistung teurer als nötig?</p>
      </div>
      <div class="number-card">
        <div class="number-big">Kernproblem</div>
        <p>In vielen Fällen sind weder Markt noch Kostenstruktur der Kern.</p>
        <p class="number-source">Die Geschäftsarchitektur passt nicht zur Denklogik des Unternehmers.</p>
=======
    <p class="section-label">Das ist es</p>
    <h2>Expertensystem für strategische Selbstführung</h2>
    <p class="section-intro">Human-System-Fit ist kein Coaching. Und keine klassische Beratung.</p>

    <div class="numbers-grid">
      <div class="number-card card-dark" style="grid-column: span 8;">
        <div class="number-big" style="font-size:clamp(48px,6vw,72px);">Menschlicher</div>
        <p style="font-size:1.08rem;"><strong>als Unternehmensberatung.</strong> Weil nicht nur Strategie zählt, sondern auch Rolle, Energie, Kapazität und Führungsrealität.</p>
      </div>
      <div class="number-card" style="grid-column: span 4;">
        <div class="number-big" style="font-size:clamp(34px,3.5vw,48px);">Härter</div>
        <p style="font-size:0.92rem;"><strong>als Coaching.</strong> Weil es nicht bei Reflexion bleibt, sondern auf Business Case, Struktur und echte Entscheidungen zielt.</p>
      </div>
      <div class="number-card">
        <div class="number-big" style="font-size:clamp(34px,3.5vw,48px);">Konkreter</div>
        <p><strong>als eine Potenzialanalyse.</strong><br>Weil am Ende nicht nur Erkenntnis steht, sondern eine reale Richtung für die nächsten Entscheidungen.</p>
      </div>
      <div class="number-card card-dark">
        <div class="number-big">Du bekommst...</div>
        <p><strong>keinen Input-Overload.</strong> Du bekommst einen strukturierten Entscheidungsraum für das, was als Nächstes wirklich zählt.</p>
>>>>>>> feature/website-update-2026-04-20
      </div>
    </div>
  </div>
</section>

<hr class="divider-line">

<!-- ============================================================
     SYSTEM VISUALISIERUNG
============================================================ -->
<section class="section system-section">
  <div class="container">
    <p class="section-label" style="color:#d4c86a;">Wechselwirkungen</p>
    <h2>Wo entsteht Reibung?</h2>
    <p class="section-intro">Wenn Angebot, Entscheidungslogik, Arbeitsweise, Energiehaushalt und Marktanforderungen nicht aufeinander abgestimmt sind, entsteht strukturelle Reibung – oft spürbar, bevor sie messbar wird.</p>

    <div class="orbit-wrapper">
      <div class="orbit-ring"></div>
      <div class="orbit-center">
        <span class="center-main">DU</span>
        <span class="center-hover">Im Kopf des Unternehmers läuft die Struktur zusammen. Wenn du Reibung spürst, wirkt sich das auf alles andere aus.</span>
      </div>

      <div class="orbit-card">
        <strong>Angebot</strong>
        Wert · Format · Preis
        <div class="card-desc">Passt dein Angebot zur aktuellen Marktwahrnehmung und zu dem, was du leisten willst?</div>
      </div>
      <div class="orbit-card">
        <strong>Markt</strong>
        Bedarf · Resonanz
        <div class="card-desc">Gibt es eine klare Übereinstimmung zwischen deiner Positionierung und dem, was der Markt sucht?</div>
      </div>
      <div class="orbit-card">
        <strong>Systeme</strong>
        Reibung · Flow
        <div class="card-desc">Unterstützen deine Tools und Prozesse deine Arbeitsweise – oder stehen sie ihr im Weg?</div>
      </div>
      <div class="orbit-card">
        <strong>Ressourcen</strong>
        Zeit & Energie
        <div class="card-desc">Steht dein Einsatz in einem sauberen Verhältnis zum Ergebnis, das du erzielst?</div>
      </div>
      <div class="orbit-card">
        <strong>Entscheidungen</strong>
        Reihenfolge · Klarheit
        <div class="card-desc">Hast du eine klare Grundlage für die Reihenfolge deiner Entscheidungen?</div>
      </div>
      <div class="orbit-card">
        <strong>Führung</strong>
        Denklogik · Haltung
        <div class="card-desc">Spiegelt dein Führungsstil das wider, was dein Geschäftsmodell tatsächlich braucht?</div>
      </div>
    </div>

    <p class="system-note">Fahre über eine Karte – um die Wechselwirkung zu sehen</p>
  </div>
</section>

<hr class="divider-line">

<!-- ============================================================
     ERGEBNISBEREICH
============================================================ -->
<section class="section">
  <div class="container">
<<<<<<< HEAD
    <p class="section-label">Nach dem Human-System-Fit</p>
    <h2>Was danach anders ist</h2>
    <p class="section-intro">Keine Versprechen. Keine Garantien. Aber eine klare Einordnung, mit der Du besser entscheiden kannst.</p>

    <div class="result-grid">
      <div class="result-card">
        <h3>Du siehst, was wirklich los ist</h3>
        <p>Nicht nur Vermutungen, sondern ein klarer Blick auf die strukturellen Ursachen in Deinem Business.</p>
      </div>
      <div class="result-card">
        <h3>Du kennst den nächsten Schritt</h3>
        <p>Eine tragfähige Entscheidung statt zwanzig offener Baustellen gleichzeitig.</p>
      </div>
      <div class="result-card">
        <h3>Du kannst bewusst weglassen</h3>
        <p>Ohne Angst, etwas Wichtiges zu verpassen. Fokus statt Daueraktivität.</p>
=======
    <p class="section-label">Die Logik dahinter</p>
    <h2>Die beste Lösung ist immer, was am besten zu dir passt.</h2>
    <p class="section-intro">Keine Versprechen.<br>Keine Garantien.<br>Aber eine klare Einordnung, mit der Du besser entscheiden kannst.</p>

    <div class="result-grid">
      <div class="result-card">
        <h3>Human-System-Fit</h3>
        <p>Der Einstieg für Unternehmer vor einer wichtigen Entscheidung</p>
        <ul style="margin-bottom:1.2rem;">
          <li>15–20 Minuten strukturierte Reflexion</li>
          <li>Einordnung von Reibung, Prioritäten und nächstem Schritt</li>
          <li>Fokus auf Business, Rolle, Energie, Markt und Entscheidungslogik</li>
        </ul>
        <div style="margin-bottom:0.5rem;"><strong>Ergebnis:</strong></div>
        <p>Du weißt, was gerade wirklich trägt — und was du bewusst weglassen kannst.</p>
      </div>
      <div class="result-card">
        <h3>Sparring</h3>
        <p>Wenn du die Einordnung unter Realitätsdruck stabil umsetzen willst</p>
        <ul style="margin-bottom:1.2rem;">
          <li>3 × 60 Minuten Videocalls</li>
          <li>asynchrone Begleitung über 6 Wochen</li>
          <li>Schärfung von Reihenfolge, Führung, Struktur und Umsetzungslogik</li>
        </ul>
        <div style="margin-bottom:0.5rem;"><strong>Ergebnis:</strong></div>
        <p>Klarheit und nachhaltige Integration in deinen Alltag.</p>
      </div>
      <div class="result-card">
        <h3>AI-Fit Audit</h3>
        <p>Bevor KI in deinem Business Tempo macht, sollte klar sein, ob sie das Richtige beschleunigt.</p>
        <ul style="margin-bottom:1.2rem;">
          <li>Wo spart KI wirklich Zeit, Kosten oder operative Last?</li>
          <li>Welche Prozesse sind reif für Automatisierung?</li>
          <li>Wo braucht es klare menschliche Entscheidung?</li>
        </ul>
        <div style="margin-bottom:0.5rem;"><strong>Ergebnis:</strong></div>
        <p>Eine klare Mensch-KI-Rollenlogik für dein Business – bevor aus Fortschritt teurer Aktionismus wird.</p>
>>>>>>> feature/website-update-2026-04-20
      </div>
    </div>

    <div class="hero-thesis result-thesis">
      <p>
        <span class="result-thesis-label">Das Ergebnis</span>
<<<<<<< HEAD
        <strong>Weniger Lehrgeld</strong><br />
=======
        <strong style="color:#fff;">Vorher Klarheit – statt nachher Lehrgeld</strong><br />
>>>>>>> feature/website-update-2026-04-20
        Du investierst Zeit, Geld und Energie in die Reihenfolge, die wirklich trägt.
      </p>
    </div>

    <div class="consequence-grid" style="margin-top: var(--gap);">
<<<<<<< HEAD
      <div class="consequence-col col-before">
=======
      <!-- Kacheln entfernt wie gewünscht -->
    </div>
  </div>
</section>

<hr class="divider-line">

<!-- ============================================================
     PRICING
============================================================ -->
<section class="section">
  <div class="container">
    <p class="section-label">Vor der nächsten Investition</p>
    <h2>Erst Klarheit. Dann investieren.</h2>
    <p class="section-intro">Du prüfst, was in Deiner Situation zuerst trägt. So minimierst Du Entscheidungsrisiko, bevor Aufwand entsteht.</p>

    <div class="pricing-block">
      <div class="consequence-col col-before" style="margin:0 1rem 0 0;">
>>>>>>> feature/website-update-2026-04-20
        <h3>Ohne Einordnung</h3>
        <ul>
          <li>Daueranspannung</li>
          <li>Teure Korrekturschleifen</li>
          <li>Entscheidungsstau</li>
          <li>Mentale Erschöpfung</li>
        </ul>
      </div>
      <div class="consequence-col col-after">
        <h3>Mit Einordnung</h3>
        <ul>
          <li>Tragfähige Entscheidungsarchitektur</li>
          <li>Sauberer Ressourceneinsatz</li>
          <li>Stimmiges Wachstum</li>
          <li>Unternehmerische Ruhe</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<hr class="divider-line">

<!-- ============================================================
<<<<<<< HEAD
     PRICING
============================================================ -->
<section class="section">
  <div class="container">
    <p class="section-label">Vor der nächsten Investition</p>
    <h2>Erst Klarheit. Dann investieren.</h2>
    <p class="section-intro">Du prüfst, was in Deiner Situation zuerst trägt. So minimierst Du Entscheidungsrisiko, bevor Aufwand entsteht.</p>

    <div class="pricing-block">
      <div class="price-card price-active">
        <p class="price-label">Human-System-Fit</p>
        <div class="price-tag">15–20 Minuten</div>
        <h3>Strukturierte Einordnung</h3>
        <p>Der niedrigschwellige Einstieg, um vor der nächsten Entscheidung Klarheit zu bekommen.</p>
        <div class="checkmarks">
          <span class="checkmark-item">Kein Abo</span>
          <span class="checkmark-item">Keine Folgekosten</span>
          <span class="checkmark-item">Klare Einordnung</span>
        </div>
      </div>
      <div class="price-card">
        <p class="price-label">Sparring</p>
        <div class="price-tag" style="color:var(--primary);">3 × 60 Minuten</div>
        <h3>6 Wochen Begleitung</h3>
        <p>Wenn Du die Einordnung in die Umsetzung bringen und unter Realitätsdruck stabil halten willst.</p>
        <div class="checkmarks">
          <span class="checkmark-item">3 × 60 Minuten</span>
          <span class="checkmark-item">Asynchrone Begleitung</span>
          <span class="checkmark-item">Nur mit Human-System-Fit</span>
        </div>
      </div>
    </div>
  </div>
</section>

<hr class="divider-line">

<!-- ============================================================
     ABSCHLUSS / BOT
============================================================ -->
<section class="section closing-section" id="start">
  <div class="container">
    <div class="closing-inner">
      <p class="section-label" style="color: var(--gold);">Bevor Du weiter investierst</p>
      <h2>Du musst nicht entscheiden wie ein Konzern.</h2>
      <p>Du brauchst ein System, das Deine schnellen Entscheidungen belastbar macht.</p>
      <p>Starte den Human-System-Fit. 15–20 Minuten. Klare nächste Schritte. Kein Risiko-Blindflug.</p>
      <div class="checkmarks" style="justify-content:center; margin: 1.25rem 0 0;">
        <span class="checkmark-item">Entscheidungsrisiko minimieren</span>
        <span class="checkmark-item">Keine Folgekosten</span>
        <span class="checkmark-item">Kein Abo</span>
      </div>

      <div class="bot-placeholder">
        <iframe
          src="https://bot.mariowittmer.de/business-system-fit-v-3-4-1-r2vcd4e"
          style="width: 100%; height: 780px; border: 0;"
          loading="lazy"
        ></iframe>
      </div>
    </div>
=======
     ABSCHLUSS / BOT
============================================================ -->
<section class="section closing-section" id="start">
  <div class="container" style="width:fit-content;max-width:100vw;">
    <div class="closing-inner">
      <p class="section-label" style="color: var(--gold);">Klarheit durch Reflexion</p>
      <h2 style="display:block; max-width: 900px;">Betrachte dein Business in ca. 15&nbsp;Minuten durch 8 Perspektiven</h2>
      <p>
        Was willst du bewegen?<br>
        Deine Antworten öffnen den Raum für echte, tragfähige Veränderungen.<br><br>
        Du musst jetzt nichts optimieren. Du solltest nur wissen, welche Optionen dich wirklich tragen, damit sich dein Business rund anfühlt.<br>
        <span style="display:inline-block; margin-top:0.7em;">
          <span style="color:#2563eb;font-weight:700;">✓</span> klare Einordnung
          <span style="margin:0 0.7em;"></span>
          <span style="color:#2563eb;font-weight:700;">✓</span> Keine Folgekosten
          <span style="margin:0 0.7em;"></span>
          <span style="color:#2563eb;font-weight:700;">✓</span> Kein Abo
        </span>
      </p>
      <iframe
        src="https://bot.mariowittmer.de/human-system-fit"
        style="background:#2563eb; border-radius:18px; width:100%; max-width:700px; height:780px; border:0; display:block; margin:0 auto;"
        loading="lazy"
      ></iframe>
>>>>>>> feature/website-update-2026-04-20
  </div>
</section>

<!-- ============================================================
     FOOTER
============================================================ -->
<footer class="site-footer">
  <div class="container">
    <div class="footer-inner">
      <div class="footer-logo">© 2026 · Business Architekt</div>
      <nav class="footer-links">
        <a href="/impressum">Impressum</a>
        <a href="/datenschutz">Datenschutz</a>
        <a href="/agb">AGB</a>
      </nav>
    </div>
  </div>
</footer>

<<<<<<< HEAD
=======
<div id="bot-overlay" style="display:none; position:fixed; z-index:9999; top:0; left:0; width:100vw; height:100vh; background:rgba(16,7,70,0.82); backdrop-filter:blur(2px); align-items:center; justify-content:center;">
  <div style="position:relative; background:#2563eb; border-radius:18px; box-shadow:0 8px 32px rgba(16,7,70,0.18); padding:0; max-width:720px; width:95vw;">
    <button id="close-bot-overlay" style="position:absolute; top:10px; right:18px; background:transparent; border:none; color:#fff; font-size:2rem; cursor:pointer; z-index:10;">&times;</button>
    <iframe
      src="https://bot.mariowittmer.de/human-system-fit"
      style="background:#2563eb; border-radius:18px; width:100%; height:78vh; min-height:420px; max-height:820px; border:0; display:block;"
      loading="lazy"
    ></iframe>
  </div>
</div>
<script>
  const openBtn = document.getElementById('open-bot-overlay');
  const overlay = document.getElementById('bot-overlay');
  const closeBtn = document.getElementById('close-bot-overlay');
  if(openBtn && overlay && closeBtn) {
    openBtn.addEventListener('click', () => { overlay.style.display = 'flex'; });
    closeBtn.addEventListener('click', () => { overlay.style.display = 'none'; });
    overlay.addEventListener('click', (e) => { if(e.target === overlay) overlay.style.display = 'none'; });
  }
</script>

>>>>>>> feature/website-update-2026-04-20
</body>
</html>
